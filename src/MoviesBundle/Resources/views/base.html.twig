<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <link rel="stylesheet" href="{{ asset('css/app.css') }}">

    <!-- import de bootstrap -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">

    <title>{% block title %}Films{% endblock %}</title>

    {% block js %}{% endblock %}

</head>
<body>

<div class="container">
    <header>
        <table class="test">
            <tr>
                <td>
                    <a href="{{ path('homepage') }}"> <img src="{{ asset('img/logo2.jpg') }}" alt="Logo" width="300px" height="auto"/> </a>
                </td>
                <td>
                    <h1>Films</h1>
                </td>
            </tr>
        </table>
    </header>

    {# message flash #}
    {%  for label, messages in app.flashes %}
        <div class="alert alert-{{ label }}">
            {% for message in messages %}
                {{ message }}
            {% endfor %}
        </div>
    {%  endfor %}

    {% block content %}{% endblock %}

    <hr>

    <footer>
        <table>
            <tr>
                <td><a href="{{ path('movie_admin') }}">Gérer films</a></td>
            </tr>
            <tr>
                {% if app.user %}
                    <td><a href="">Mon compte</a></td>
                    <td><a href="">Déconnexion</a></td>
                {% else %}
                    <td><a href="">Connexion</a></td>
                    <td><a href="">Inscription</a></td>
                {% endif %}
            </tr>
        </table>
    </footer>
</div>
</body>
</html>